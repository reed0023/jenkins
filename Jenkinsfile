Jenkinsfile (Declarative Pipeline)
pipeline {
    agent any 
        stages {
            stage('One') {
                steps {
                    sh 'python --version'
                    echo("Hello wrold");
                }
            }   
        }
    }
